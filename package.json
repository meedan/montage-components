{
  "name": "montage-components",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/meedan/montage-components.git"
  },
  "scripts": {
    "clean": "npx oao clean",
    "deploy": "now --target=production --no-clipboard --public --token=$NOW_TOKEN"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "now": "^15.0.6"
  }
}
